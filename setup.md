# دليل الإعداد الشامل
# Complete Setup Guide

## 📋 جدول المحتويات

1. [المتطلبات](#المتطلبات)
2. [التثبيت المحلي](#التثبيت-المحلي)
3. [إعداد Supabase](#إعداد-supabase)
4. [إعداد ملفات الصوت](#إعداد-ملفات-الصوت)
5. [التشغيل المحلي](#التشغيل-المحلي)
6. [النشر على Vercel](#النشر-على-vercel)
7. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## المتطلبات

### البرامج المطلوبة

- **Node.js** 18.0 أو أحدث
  - تحميل من: https://nodejs.org/
  - تحقق من الإصدار: `node --version`

- **npm** أو **yarn**
  - يأتي مع Node.js
  - تحقق من الإصدار: `npm --version`

- **Git**
  - تحميل من: https://git-scm.com/
  - تحقق من الإصدار: `git --version`

- **حساب Supabase**
  - إنشاء حساب مجاني على: https://supabase.com

- **حساب Vercel** (للنشر)
  - إنشاء حساب مجاني على: https://vercel.com

---

## التثبيت المحلي

### الخطوة 1: استنساخ المشروع

```bash
# استنساخ المشروع من GitHub
git clone <repository-url>

# الدخول إلى مجلد المشروع
cd medical-queue-management-system
```

### الخطوة 2: تثبيت المكتبات

```bash
# استخدام npm
npm install

# أو استخدام yarn
yarn install
```

### الخطوة 3: إعداد متغيرات البيئة

```bash
# نسخ ملف المثال
cp .env.local.example .env.local
```

ثم عدّل ملف `.env.local` وأضف بيانات Supabase:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
```

---

## إعداد Supabase

### الخطوة 1: إنشاء مشروع Supabase

1. اذهب إلى https://app.supabase.com
2. اضغط "New Project"
3. أدخل اسم المشروع
4. اختر كلمة مرور قوية
5. اختر المنطقة الجغرافية
6. اضغط "Create new project"

### الخطوة 2: الحصول على مفاتيح API

1. اذهب إلى "Settings" → "API"
2. انسخ:
   - `Project URL` → `NEXT_PUBLIC_SUPABASE_URL`
   - `anon public` → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `service_role secret` → `SUPABASE_SERVICE_ROLE_KEY`

### الخطوة 3: تطبيق الـ Schema

#### الطريقة الأولى: SQL Editor

1. اذهب إلى "SQL Editor"
2. اضغط "New Query"
3. انسخ محتوى ملف `supabase/migrations/001_initial_schema.sql`
4. الصق الكود
5. اضغط "Run"

#### الطريقة الثانية: Supabase CLI

```bash
# تثبيت Supabase CLI
npm install -g supabase

# تسجيل الدخول
supabase login

# ربط المشروع
supabase link --project-ref your_project_ref

# تطبيق الـ migrations
supabase db push
```

### الخطوة 4: التحقق من الجداول

1. اذهب إلى "Table Editor"
2. تحقق من وجود جميع الجداول:
   - centers
   - screens
   - clinics
   - queue_calls
   - doctors
   - appointments
   - patients
   - consultations
   - complaints
   - leave_requests
   - attendance_records
   - admin_users
   - notifications

---

## إعداد ملفات الصوت

### إنشاء مجلد الصوت

```bash
# إنشاء مجلد الصوت
mkdir -p public/audio
```

### ملفات الصوت المطلوبة

ضع الملفات التالية في مجلد `public/audio/`:

#### 1. صوت التنبيه
- `ding.mp3` - صوت التنبيه الأساسي

#### 2. أرقام المرضى (1-200)
- `1.mp3` - "على العميل رقم واحد"
- `2.mp3` - "على العميل رقم اثنين"
- ...
- `200.mp3` - "على العميل رقم مائتين"

#### 3. أسماء العيادات
- `clinic1.mp3` - "التوجه إلى عيادة طب الأسرة"
- `clinic2.mp3` - "التوجه إلى عيادة الأسنان"
- `clinic3.mp3` - "التوجه إلى عيادة العيون"
- `clinic4.mp3` - "التوجه إلى عيادة الجلدية"
- `clinic5.mp3` - "التوجه إلى عيادة الأطفال"

#### 4. رسائل فورية
- `instant1.mp3` - "أهلا وسهلا بكم في المركز الطبي"
- `instant2.mp3` - "رجاء الانتظار بهدوء"
- `instant3.mp3` - "شكراً لتعاونكم معنا"
- ...

### إنشاء ملفات الصوت

يمكنك استخدام أي من الخدمات التالية:

#### Google Text-to-Speech
```bash
# استخدام Google Cloud Text-to-Speech API
# أو استخدام أداة أونلاين: https://ttsmp3.com/
```

#### Azure Speech Services
```bash
# استخدام Azure Cognitive Services
# https://azure.microsoft.com/en-us/services/cognitive-services/text-to-speech/
```

#### محلي (استخدام ffmpeg)
```bash
# تثبيت ffmpeg
# macOS: brew install ffmpeg
# Ubuntu: sudo apt-get install ffmpeg
# Windows: choco install ffmpeg

# تحويل ملفات WAV إلى MP3
ffmpeg -i input.wav -q:a 9 -n output.mp3
```

---

## التشغيل المحلي

### الخطوة 1: بدء خادم التطوير

```bash
npm run dev
# أو
yarn dev
```

### الخطوة 2: فتح التطبيق

افتح المتصفح وذهب إلى:
```
http://localhost:3000
```

### الخطوة 3: اختبار الصفحات

#### الصفحة الرئيسية
```
http://localhost:3000/
```

#### لوحة التحكم
```
http://localhost:3000/admin
```
- البريد: `admin@example.com`
- كلمة المرور: `password123`

#### شاشة العرض
```
http://localhost:3000/display
```

#### لوحة التحكم بالعيادة
```
http://localhost:3000/control
```

#### صفحة العميل
```
http://localhost:3000/client
```

#### حجز المواعيد
```
http://localhost:3000/appointments
```

#### الطباعة
```
http://localhost:3000/print
```

#### الأطباء
```
http://localhost:3000/doctors
```

#### الاستشارات
```
http://localhost:3000/consultations
```

#### عن البرنامج
```
http://localhost:3000/about
```

---

## النشر على Vercel

### الخطوة 1: إعداد GitHub

```bash
# إضافة جميع الملفات
git add .

# إنشاء commit
git commit -m "Initial commit: Medical Queue Management System"

# رفع إلى GitHub
git push origin main
```

### الخطوة 2: ربط مع Vercel

1. اذهب إلى https://vercel.com
2. اضغط "New Project"
3. اختر مستودع GitHub الخاص بك
4. اضغط "Import"

### الخطوة 3: إضافة متغيرات البيئة

في صفحة الإعدادات، أضف المتغيرات التالية:

```
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

### الخطوة 4: النشر

1. اضغط "Deploy"
2. انتظر انتهاء النشر
3. سيتم إعطاؤك رابط الموقع

### الخطوة 5: التحديثات التلقائية

بعد الآن، كل push إلى `main` سيؤدي إلى نشر تلقائي على Vercel.

---

## استكشاف الأخطاء

### المشكلة: "Cannot find module"

**الحل:**
```bash
# حذف node_modules
rm -rf node_modules

# إعادة تثبيت المكتبات
npm install
```

### المشكلة: "Supabase connection failed"

**الحل:**
1. تحقق من صحة مفاتيح API
2. تحقق من اتصال الإنترنت
3. تحقق من أن Supabase project نشط

### المشكلة: "Audio files not found"

**الحل:**
1. تأكد من وجود مجلد `public/audio/`
2. تأكد من وجود ملفات الصوت
3. تحقق من أسماء الملفات

### المشكلة: "Arabic text not displaying correctly"

**الحل:**
1. تحقق من أن `dir="rtl"` موجود في HTML
2. تحقق من أن خط Cairo محمل
3. تحقق من ترميز UTF-8

### المشكلة: "Realtime updates not working"

**الحل:**
1. تحقق من أن Realtime مفعل في Supabase
2. تحقق من اتصال WebSocket
3. تحقق من RLS policies

### المشكلة: "Print not working"

**الحل:**
1. تحقق من أن المتصفح يدعم الطباعة
2. جرب متصفح آخر
3. تحقق من وحدة التحكم (Console) للأخطاء

---

## نصائح مهمة

### الأمان
- ✅ لا تشارك مفاتيح API العامة
- ✅ استخدم متغيرات البيئة
- ✅ فعّل RLS في Supabase
- ✅ استخدم كلمات مرور قوية

### الأداء
- ✅ استخدم CDN للملفات الثابتة
- ✅ فعّل الضغط (gzip)
- ✅ استخدم الـ caching
- ✅ حسّن صور الويب

### الصيانة
- ✅ احتفظ بنسخ احتياطية من البيانات
- ✅ راقب الأخطاء والسجلات
- ✅ حدّث المكتبات بانتظام
- ✅ اختبر التحديثات قبل النشر

---

## الدعم والمساعدة

### الموارد المفيدة
- [Next.js Documentation](https://nextjs.org/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [TypeScript Documentation](https://www.typescriptlang.org/docs)

### التواصل
- البريد الإلكتروني: support@medical-queue.com
- الموقع: https://medical-queue.example.com

---

**آخر تحديث:** 2024
**الإصدار:** 1.0.0
